# Database connection string (required)
# Example: postgres://user:password@host:port/database
DATABASE=postgres://postgres@localhost:5432/starcraft2

# Player names to filter for result determination (optional, comma-separated, empty = all players)
# Example: PlayerOne,PlayerTwo,PlayerThree
PLAYERS=MuNi,Pineapple,SINDIOS

# Replay file paths to scan (required, comma-separated)
# NOTE: Use quotes if paths contain spaces
# Example: "/path/to/replays1,/another path/replays2"
REPLAYS="/home/mahendra/Windows/Documents and Settings/Name/Documents/StarCraft II/Accounts/"

# Number of concurrent worker goroutines (optional, default: number of CPUs)
# Example: 12
WORKERS=12
