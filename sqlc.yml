version: "2"
sql:
  - engine: "postgresql"
    gen:
      go:
        out: "models"
        package: "models"
        sql_package: "pgx/v5"
    queries:
      - "sqlc/queries.sql"
    schema:
      - "sqlc/schema.sql"
